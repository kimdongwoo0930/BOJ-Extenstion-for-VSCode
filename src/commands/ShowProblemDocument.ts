import * as vscode from "vscode";

/**
 * @TItle 문제 문서 보여주기
 * @description 해당 문제를 가져와 웹뷰를 통해 전체적인 문제를 보여주기위한 함수이다.
 * @param context vscode.ExtensionContext
 */
export const showProblem = (context: vscode.ExtensionContext) => {};
